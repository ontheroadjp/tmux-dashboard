# Backend production profile (.env.prod.example)
# DASHBOARD_PORT: App port value (production convention: 10323).
DASHBOARD_PORT=10323
# DASHBOARD_AUTH_USER: Login username for dashboard API (example: prod-user).
DASHBOARD_AUTH_USER=<PROD_USER>
# DASHBOARD_AUTH_PASSWORD: Login password for dashboard API (example: strong password).
DASHBOARD_AUTH_PASSWORD=<PROD_PASSWORD>
# DASHBOARD_AUTH_SECRET: Token signing secret (long random string required in production).
DASHBOARD_AUTH_SECRET=<PROD_SECRET>
# DASHBOARD_AUTH_TOKEN_TTL_SEC: Access token lifetime in seconds (example: 86400 = 24h).
DASHBOARD_AUTH_TOKEN_TTL_SEC=86400
# DASHBOARD_ALLOWED_ACTIONS: Allowed command scope (example: all).
DASHBOARD_ALLOWED_ACTIONS=all
# DASHBOARD_DEBUG: Debug mode flag in production (1=enabled, 0=disabled).
DASHBOARD_DEBUG=0
# DASHBOARD_CORS_ORIGINS: Allowed browser origins (comma-separated when multiple).
DASHBOARD_CORS_ORIGINS=https://tmux.example.com
# GUNICORN_WORKERS (optional): Worker count for gunicorn (example: 2).
# GUNICORN_WORKERS=2

# Backend development profile (.env.dev.example)
# DASHBOARD_PORT: Backend listen port in development (example: 5001).
DASHBOARD_PORT=5001
# DASHBOARD_AUTH_USER: Login username for dashboard API (example: dev-user).
DASHBOARD_AUTH_USER=<DEV_USER>
# DASHBOARD_AUTH_PASSWORD: Login password for dashboard API (example: dev-password).
DASHBOARD_AUTH_PASSWORD=<DEV_PASSWORD>
# DASHBOARD_AUTH_SECRET (optional): Fixed token signing secret for stable sessions.
# Example: DASHBOARD_AUTH_SECRET=dev-long-random-secret
# DASHBOARD_AUTH_SECRET=<DEV_SECRET>
# DASHBOARD_AUTH_TOKEN_TTL_SEC: Access token lifetime in seconds (example: 86400 = 24h).
DASHBOARD_AUTH_TOKEN_TTL_SEC=86400
# DASHBOARD_ALLOWED_ACTIONS: Allowed command scope (example: all).
DASHBOARD_ALLOWED_ACTIONS=all
# DASHBOARD_DEBUG: Debug mode flag in dev (1=enabled, 0=disabled).
DASHBOARD_DEBUG=1
# DASHBOARD_CORS_ORIGINS: Allowed browser origins (comma-separated when multiple).
DASHBOARD_CORS_ORIGINS=http://127.0.0.1:4000
